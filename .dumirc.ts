import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/ant-table-excel/',
  publicPath: '/ant-table-excel/',
  outputPath: 'docs-dist',
  mfsu: false,
  themeConfig: {
    name: 'ant-table-excel',
  },
});
